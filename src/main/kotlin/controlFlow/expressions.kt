fun main() {
    //diff between expressions and statements:

    //statement :
    /*val openOffice = 7
      val now = 8
        if (now > openOffice)
            print("Office already open")
        else
            print("Office close")
     */

    //expression :
    //statement that can store variable and can return the value
    //example:

    val angka = 1
    val angka2 = 2

    val result = if(angka>angka2) "benar" else "salah"
    println(result)

}